
To build and test SynPSG_PDF on Linux:

1. Open a BASH shell and cd to this directory

2. Execute the script to deploy the libHaru shared object library:

      . DeployHaru.sh

   You should see output like this (for a 64-bit system):

      Extracting the 64-bit libHaru shared object library...
      Copying shared object libhpdf64.so library to /synergy/1031/dbl/bin

3. Execute the script to build the SynPSG_PDF code

      .  BuildPdf.sh

   You should see output like this:

      Prototyping PDF code...
      Compiling PDF code...
      Linking executable library PDF_EXE:SynPSG_PDF.elb...

4. Execute the script to build the example programs:

      .  BuildExamples.sh

   You should see output like this:

      Building example programs...
      - AutoItems.dbl
      - BlueAndRedBox.dbl
      - DblMessagesReport.dbl
      - DeliveryTicket.dbl
      - DesignGrid.dbl
      - HeaderBodyFooter.dbl
      - Landscape132.dbl
      - MixedPageOrientation.dbl
      - Portrait100.dbl

5. Execute the script to run the example programs:

      .  RunExamples.sh

   You should see output like this:

      Running example programs...
      - AutoItems.DBR
      - BlueAndRedBox.DBR
      - DblMessagesReport.DBR
      - DeliveryTicket.DBR
      - DesignGrid.DBR
      - HeaderBodyFooter.DBR
      - Landscape132.DBR
      - MixedPageOrientation.DBR
      - Portrait100.DBR

      Example PDF files are in the out folder

